Проверить
generate_secrets/Pipfile
учтывая
import base64
import secrets
import hashlib
ведь You can replace secrets in dst/.env with ones generated. You can generate secrets running

```sh
pip3 install pipenv
cd ./generate_secrets
pipenv shell
python ./generate_secrets.py
```
------
Удалить лишнее и поменять нужное в src

------
