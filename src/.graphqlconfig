{
  "name": "GraphQL Schema",
  "schemaPath": "schema.graphqls",
  "extensions": {
    "endpoints": {
      "localhost": {
        "url": "http://localhost:80/query",
        "headers": {
          "Authorization": "Bearer ${env:JWT-TOKEN}",
          "user-agent": "JS GraphQL"
        },
        "introspect": false
      },
      "debianVM": {
        "url": "http://192.168.1.69:80/query",
        "headers": {
          "Authorization": "Bearer ${env:JWT-TOKEN}",
          "user-agent": "JS GraphQL"
        },
        "introspect": false
      }
    }
  }
}